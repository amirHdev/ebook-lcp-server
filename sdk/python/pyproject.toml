[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ebook-lcp-server-sdk"
version = "0.1.0"
description = "Small Python client for the ebook-lcp-server API"
requires-python = ">=3.10"
